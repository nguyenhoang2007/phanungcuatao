#include <bits/stdc++.h>

#define ll long long
using namespace std;

int n,b[100000],dp[100000][100]={};

void solve() {
    for(int i = 1; i < n; ++i) {
        
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> n;
    for(int i = 0; i < n; ++i) cin >> b[i];
    solve();
}