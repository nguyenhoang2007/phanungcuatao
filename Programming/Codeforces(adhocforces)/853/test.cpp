#include <bits/stdc++.h>

#define ll long long
using namespace std;
int n;

void solve() {
    int a[n] = {2};
    for(int i = 0; i < n; ++i) cout << a[i] << " ";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    //initialize
    cin >> n;
    solve();
}