#include <bits/stdc++.h>

#define ll long long
using namespace std;

void solve() {
    int n;
    cin >> n;
     cout << ceil(sqrt(n));
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solve();
}