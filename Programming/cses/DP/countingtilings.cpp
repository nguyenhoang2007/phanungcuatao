#include <bits/stdc++.h>

#define ll long long
using namespace std;
int n,m,a[10][1000];
void solve() {
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> n >> m;
    for(int i = 0; i < n; ++i) 
        for(int j = 0; j < m; ++j) cin >> a[i][j];
    solve();
}