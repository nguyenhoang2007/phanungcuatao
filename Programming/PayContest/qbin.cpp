#include <bits/stdc++.h>

#define ll long long
using namespace std;

map<string,int> d;

string s;

void solve() {
    for(int i = 0; i < s.size(); ++i) {
        
    }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> s;
    d[s]++;
    solve();
}