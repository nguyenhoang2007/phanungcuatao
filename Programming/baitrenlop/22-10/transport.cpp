#include <bits/stdc++.h>

#define ll long long
using namespace std;
int n,s,a[100000],dp[1000];
void solve() {
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    freopen("transport.inp","r",stdin);
    freopen("transport.out","w",stdout);
    cin >> n;
    for(int i = 0; i < n; ++i) cin >> a[i];
    solve();
}