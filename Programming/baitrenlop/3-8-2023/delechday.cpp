#include <bits/stdc++.h>

#define int long long
using namespace std;
int n,a[1000001],test_case;

void solve() {
    
}

int32_t main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    //freopen("delechday.inp","r",stdin);
    //freopen("delechday.out","w",stdout);
    //initialize
    while(test_case--) {
        cin >> n;
        for(int i = 1; i <= n; ++i) cin >> a[i];
        solve();
    }
}