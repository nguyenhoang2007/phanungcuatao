#include <bits/stdc++.h>

#define ll long long
using namespace std;
int n,s,a[100000],dp[100000],sum=0,p1=0,p2=0,r=-INFINITY;
void solve() {
    for()
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    //freopen("      ","r",stdin);
    //freopen("      ","w",stdout);
    cin >> n >> s;
    for(int i = 0; i < n; ++i) cin >> a[i];
    solve();
}